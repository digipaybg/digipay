export default defineNuxtRouteMiddleware((to, from) => {
  // console.log("Route middleware", to.path, from.path);
  // if (to.path !== from.path && import.meta.client) {
  //   window.scrollTo({
  //     top: 0,
  //     behavior: "instant",
  //   });
  // }
});
